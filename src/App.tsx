import './App.css';
import SignUpForm from '@features/SignUpForm';

const App = () => {
  return (
    <div className="App">
      <SignUpForm onSubmit={() => {}} />
    </div>
  );
};

export default App;
